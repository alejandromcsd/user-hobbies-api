import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'User Hobbies API',
  description: 'API for interacting with user hobbies',
  version: '1.0',
  tags: ['Users', 'Hobbies'],
};
